%h1 Listing orders

- if @orders.count == 0
  .row
    .col-lg-7
      %h3 There are still no orders to list
- else
  .row
    .col-lg-7
      %table.table
        %thead
          %tr
            %th Buyer Name
            %th Buyer Email
            %th Contributors
            %th Products
            %th Total €

        - @orders.each do |order|
          %tbody
            %tr
              %td= order.buyer_name
              %td= order.buyer_email
              %td= order.contributors
              %td= order.order_items.map{|oi| oi.product.name}.join(",")
              %td= order.subtotal
