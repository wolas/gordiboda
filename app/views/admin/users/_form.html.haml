= simple_form_for [:admin, @user] do |f|
  = render "admin/admin/errors", item: @user

  .form-inputs
    = f.input :name
    = f.input :email
    = f.input :password


  .form-actions
    = f.submit 'Save'

= link_to 'Back', :back