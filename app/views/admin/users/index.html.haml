%h1 Listing users

.row
  .col-lg-6
    %table.table
      %thead
        %tr
          %th.left Name
          %th.left Email
          %th

      - @users.each do |user|
        %tbody
          %tr.clickable{data: {url: url_for([:admin, user])}}
            %td.left= user.name
            %td.left= user.email
            - if user.id == current_user.id
              %td= link_to 'Edit', edit_admin_user_path(current_user.id)

      %tr.active
        %td.left
        %td.left
        %td= link_to 'New', new_admin_user_path


