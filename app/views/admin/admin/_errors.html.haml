- if item.errors.any?
  #error_explanation
    %h2= "#{pluralize(item.errors.count, "error")} prohibited this #{item.class.to_s.downcase} from being saved:"
    %ul
      - item.errors.full_messages.each do |msg|
        %li= msg